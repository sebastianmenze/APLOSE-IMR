query listDetectors {
    allDetectors {
        results {
            id
            name
            configurations {
                id
                configuration
            }
        }
    }
}
